# API 参考

API 签名与 docstring 以源码为准（多为英文，部分中文），因此自动生成的 API 正文只维护**英文版**一份，避免双语重复渲染导致交叉引用冲突。

## 如何查看完整 API

1. 使用页面顶部的 **语言切换器** 切换到 **English**
2. 打开导航中的 **API Reference**

完整参考覆盖：

- 公共包 `mcbe_ws_sdk` 再导出
- `gateway` / `addon` / `flow` / `protocol` / `profiles` / `delivery` / `command`
- `config` / `logging` / `errors`
