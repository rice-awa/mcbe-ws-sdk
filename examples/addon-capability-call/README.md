# Addon Capability Call Example

Complete in-memory round trip through the `mcbe-ws-sdk` public API.

```bash
python examples/addon-capability-call/capability_call.py
```

Expected output: `{"ok":true,"greeting":"Hello, Steve"}`
